- hosts: "{{ server | default('confluent_schema_registry') }}"
  sudo: yes
  tasks:
    - name: stop_schema_registry
      shell: timeout 10 supervisorctl stop schema-registry
      when: ansible_distribution == 'CentOS' and ansible_distribution_major_version == '6'
      register: result
      failed_when: result.rc != 0 and result.rc != 124

    - name: stop_schema_registry
      shell: supervisorctl stop schema-registry
      when: ansible_distribution == 'CentOS' and ansible_distribution_major_version == '7'
