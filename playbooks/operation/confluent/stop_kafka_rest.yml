- hosts: "{{ server | default('confluent_kafka_rest') }}"
  sudo: yes
  sudo_user: kafka
  tasks:
    - name: stop_kafka_rest
      shell: /usr/local/confluent_kafka/default/bin/kafka-rest-stop /etc/confluent/kafka-rest/kafka-rest.properties

