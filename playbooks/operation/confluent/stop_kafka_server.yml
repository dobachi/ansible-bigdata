- hosts: "{{ server | default('confluent_kafka_cluster') }}"
  sudo: yes
  sudo_user: kafka
  tasks:
    - name: stop_kafka_server
      shell: /usr/local/confluent_kafka/default/bin/kafka-server-stop /etc/kafka/server.properties
